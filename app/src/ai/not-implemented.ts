/** Placeholder thrown by contract stubs until they are implemented. Remove once nothing imports it. */
export function notImplemented(name: string): never {
  throw new Error(`${name} is not implemented yet`);
}
